import { makeStyles } from '@alveole/theme';

export const useStyles = makeStyles(() => ({
  passwordInput: {},
  visibilityButton: {
    height: '100%',
    borderRadius: 0,
    paddingTop: 0,
    paddingBottom: 0,
  },
}));
